machine:
    node:
        version: '0.12.0'
test:
    override:
        - ./bin/ci.sh:
            parallel: true
    post:
        - bash <(curl -s https://codecov.io/bash)
notify:
    webhooks:
        - url: https://bot.suddi.io/hooks/circle
